#include <stdio.h>

/**
 * main - afunction that prints no_ of argiments
 * @argc: counter of argument
 * @argv: array of strings
 * Return: 0(Success)
 *
 */
int main(int argc, char *argv[])
{
	int c = argc;

	argv = argv;
	printf("%d\n", c - 1);
	return (0);
}
