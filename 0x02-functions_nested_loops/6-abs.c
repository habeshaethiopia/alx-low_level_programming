#include "main.h"

/**
 * _abs - is afunction used to done ansolute value function
 * @n: int parameter
 * Return: the changed value n
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	else
		return (n);
}
